# Intentional Connection Backend

This is the backend service for the Intentional Connection application. It contains the APIs, middleware, routes, and database configurations necessary to power the application.

## Setup
1. Clone the repository
2. Run `npm install`
3. Configure your `.env` file based on `.env.example`
4. Start the server
